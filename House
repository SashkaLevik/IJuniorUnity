using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

public class House : MonoBehaviour
{    
    [SerializeField] private UnityEvent _perimeterBreached;
    public bool IsBreahed { get; private set; }

    public event UnityAction Breached
    {
        add => _perimeterBreached.AddListener(value);
        remove => _perimeterBreached.RemoveListener(value);
    }
   

    private void OnTriggerEnter2D(Collider2D collision)
    {
        if (collision.TryGetComponent<Thief>(out Thief thief))
        {
            IsBreahed = true;
            _perimeterBreached.Invoke();
        }
    }    

    private void OnTriggerExit2D(Collider2D collision)
    {
        IsBreahed = false;
    }
}
